NEXT_PUBLIC_SUPABASE_URL=https://qkmkxthpeapuecobahhx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=REPLACE_WITH_PUBLISHABLE_KEY
SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_SERVICE_ROLE_KEY
STRIPE_SECRET_KEY=REPLACE_WITH_STRIPE_SECRET_KEY
STRIPE_WEBHOOK_SECRET=REPLACE_WITH_STRIPE_WEBHOOK_SECRET
NEXT_PUBLIC_SITE_URL=https://910academy.com

# Kit (ConvertKit) — AOC waitlist sync (server-only, best-effort)
KIT_API_KEY=REPLACE_WITH_KIT_API_KEY
KIT_TAG_ID_AOC_WAITLIST=REPLACE_WITH_KIT_WAITLIST_TAG_ID
# Kit tag (aoc-free-live) for the free /three-levels event registrants (separate reminder sequence)
KIT_TAG_ID_AOC_FREE_LIVE=REPLACE_WITH_KIT_FREE_LIVE_TAG_ID

# Vercel Cron auth for GET /api/aoc/reconcile (Kit backlog drain). Long random string.
CRON_SECRET=REPLACE_WITH_LONG_RANDOM_STRING

# Cloudflare Turnstile secret (server). Leave unset to disable; signup honeypot +
# origin allowlist still apply. Set it (and the site key in public/aoc.html) to
# turn on bot verification. Test secret 1x0000000000000000000000000000000AA always passes.
TURNSTILE_SECRET_KEY=REPLACE_WITH_TURNSTILE_SECRET_KEY
